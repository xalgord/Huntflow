node_modules/
.svelte-kit/
build/
dist/
coverage/
playwright-report/
test-results/
.vercel/
# Nova Act / Playwright persistent profile dirs.
# Cookies and local storage from signed-in dashboards land here so a
# restarted Nova session doesn't have to re-authenticate. Treat the
# whole tree as secret.
.cache/

.env
.env.*
!.env.example

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.DS_Store
*.tsbuildinfo
.npmrc
.agentMemory
.agent/
.vercel

# Local IDE / agent tooling — not part of the project source.
.codex
.kilo/
.kiro/
.playwright-mcp/
.vscode/
speckit/

# Local-only payment provider scratch material. Keep credentials and
# helper scripts out of the repo so test-mode keys don't leak.
docs/dodo-payments-test-credentials.md
scripts/dodo-*
scripts/read-dodo-clipboard.mjs

# One-off screenshot capture, not used by the build.
example.png
powers/

# Internal planning / strategy docs — not for public consumption.
# These contain build prompts, architecture decisions, deploy runbooks,
# and migration procedures with internal project names.
brain.md
mvp.md
project.md
prompts.md
tech-spec.md
data-model.md
DEPLOY.md
docs/firebase-auth-migration/
docs/testing-checklist.md
docs/screenshots/
